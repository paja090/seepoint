'use client';
import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation';
export function ProfileForms({firstName,lastName,phone}:{firstName:string;lastName:string;phone:string}){const router=useRouter();const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');async function submit(event:FormEvent<HTMLFormElement>,action:string){event.preventDefault();setBusy(true);setMsg('');const body={action,...Object.fromEntries(new FormData(event.currentTarget))};const response=await fetch('/api/profile',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const result=await response.json();setBusy(false);setMsg(result.error??'Změna byla uložena.');if(response.ok){event.currentTarget.reset();router.refresh()}}return <div className="mt-6 grid gap-6 lg:grid-cols-2"><form className="card space-y-3" onSubmit={e=>submit(e,'contact')}><h2 className="text-xl font-bold">Upravit kontakt</h2><label className="block">Jméno<input className="input mt-1" name="firstName" defaultValue={firstName} required/></label><label className="block">Příjmení<input className="input mt-1" name="lastName" defaultValue={lastName} required/></label><label className="block">Telefon<input className="input mt-1" name="phone" defaultValue={phone}/></label><button disabled={busy} className="rounded-xl bg-slate-950 px-4 py-2 text-white disabled:opacity-50">{busy?'Ukládám…':'Uložit kontakt'}</button></form><form className="card space-y-3" onSubmit={e=>submit(e,'password')}><h2 className="text-xl font-bold">Změnit heslo</h2><label className="block">Současné heslo<input className="input mt-1" type="password" name="currentPassword" autoComplete="current-password" required/></label><label className="block">Nové heslo<input className="input mt-1" type="password" name="newPassword" minLength={12} autoComplete="new-password" required/></label><button disabled={busy} className="rounded-xl bg-slate-950 px-4 py-2 text-white disabled:opacity-50">{busy?'Ukládám…':'Změnit heslo'}</button></form>{msg&&<p className="text-sm lg:col-span-2">{msg}</p>}</div>}

'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export function AuthForm({ mode, token, purpose }: { mode: 'login' | 'forgot' | 'password'; token?: string; purpose?: 'activation' | 'reset' }) {
  const router = useRouter(); const [loading, setLoading] = useState(false); const [message, setMessage] = useState(''); const [error, setError] = useState('');
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(''); const values = Object.fromEntries(new FormData(event.currentTarget)); const endpoint = mode === 'login' ? '/api/auth/login' : mode === 'forgot' ? '/api/auth/forgot-password' : '/api/auth/set-password'; const response = await fetch(endpoint, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ ...values, token, purpose }) }); const result = await response.json().catch(() => ({})); setLoading(false); if (!response.ok) return setError(result.error ?? 'Požadavek se nepodařilo dokončit.'); if (mode === 'login') router.replace(result.redirectTo ?? '/dashboard'); else if (mode === 'password') { setMessage('Heslo bylo nastaveno. Nyní se můžete přihlásit.'); setTimeout(() => router.replace('/login'), 1200); } else setMessage(result.message); }
  return <form className="mt-6 space-y-4" onSubmit={submit}>{mode !== 'password' && <label className="block text-sm font-medium">E-mail<input className="input mt-1" name="email" type="email" autoComplete="email" required /></label>}{mode !== 'forgot' && <label className="block text-sm font-medium">{mode === 'login' ? 'Heslo' : 'Nové heslo'}<input className="input mt-1" name="password" type="password" minLength={mode === 'password' ? 12 : undefined} autoComplete={mode === 'login' ? 'current-password' : 'new-password'} required /></label>}{error && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-700" role="alert">{error}</p>}{message && <p className="rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800" role="status">{message}</p>}<button className="w-full rounded-xl bg-slate-950 px-4 py-3 font-semibold text-white disabled:opacity-50" disabled={loading}>{loading ? 'Zpracovávám…' : mode === 'login' ? 'Přihlásit' : mode === 'forgot' ? 'Odeslat pokyny' : 'Nastavit heslo'}</button></form>;
}

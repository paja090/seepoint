import { AuthForm } from '@/components/AuthForm'; export default async function Page({params}:{params:Promise<{token:string}>}){const {token}=await params;return <main className="grid min-h-screen place-items-center bg-slate-950 px-4"><section className="card w-full max-w-md"><h1 className="text-2xl font-bold">Nové heslo</h1><AuthForm mode="password" token={token}/></section></main>}

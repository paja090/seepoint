import { AuthForm } from '@/components/AuthForm'; export default async function Page({params}:{params:Promise<{token:string}>}){const {token}=await params;return <main className="grid min-h-screen place-items-center bg-slate-950 px-4"><section className="card w-full max-w-md"><h1 className="text-2xl font-bold">Aktivace účtu</h1><p className="mt-2 text-sm text-slate-500">Nastavte si bezpečné heslo.</p><AuthForm mode="password" token={token} purpose="activation"/></section></main>}

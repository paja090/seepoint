import { AppShell } from '@/components/AppShell';import { carriers } from '@/lib/mock-data';import { StatusBadge } from '@/components/StatusBadge';
export default function Occupancy(){ const rows=carriers.flatMap(c=>c.surfaces.flatMap(s=>s.occupancies.map(o=>({c,s,o})))); return <AppShell><h1 className="text-3xl font-bold mb-6">Obsazenost</h1><div className="card">{rows.map(({c,s,o})=><div className="border-b py-3" key={o.id}><b>{o.campaignName}</b> · {o.clientName}<p className="text-sm text-slate-500">{c.name} / {s.name} · {o.dateFrom} – {o.dateTo} <StatusBadge value={o.status}/></p></div>)}</div></AppShell> }
